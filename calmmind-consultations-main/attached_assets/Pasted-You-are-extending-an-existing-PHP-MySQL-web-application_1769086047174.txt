You are extending an existing PHP + MySQL web application called CalmMind Consultations.
The project already includes:

A working client login and registration system

PHP session-based authentication

A MySQL database with users and consultations tables

Role-based access control using a role field (client or admin)

Prepared statements for all database queries

Do NOT redesign the frontend.
Do NOT add extra features.
Do NOT change existing functionality unless explicitly stated.

Implement ONLY the features listed below, using simple, clear PHP and MySQL code.

1. Separate Admin Login Page

Create a dedicated admin login page (admin_login.php)

Admins must NOT be able to log in through the client login page

During login, verify that the user’s role is admin

If the role is not admin, deny access

Protect all admin pages by checking the session role

2. Contact Us Page

Create a public contact.php page with:

Name

Email

Message

Store submitted messages in a new database table called contact_messages

Validate inputs on the server side

Create an admin-only page to view submitted messages

Do NOT allow editing or deleting messages

3. Consultation Status Tracking

Extend the existing consultations table to include:

status (default: Pending)

admin_notes

Admins must be able to:

View all consultations

Update consultation status (Pending, Approved, Rejected)

Add optional admin notes

Clients must be able to:

View only their own consultations

See the status and admin notes

4. Admin Search and Filter System

Add search functionality to the admin consultation list:

Search by client name or email

Filter by consultation status

Sort consultations by submission date

Use prepared SQL statements

Keep the interface simple (form inputs only)

5. Appointment Scheduling

Extend the consultations table with:

appointment_datetime

Admins must be able to:

Assign an appointment date and time to an approved consultation only

Clients must be able to:

View the appointment date and time for approved consultations

Validate that appointment dates are in the future

General Rules

Use plain PHP (no frameworks)

Use MySQL with prepared statements

Use existing database connection files

Use existing session handling

Keep all logic simple and readable

Add comments where necessary

Do NOT implement email notifications

Do NOT add APIs

Do NOT add JavaScript frameworks

Output Requirements

Provide:

Required SQL statements (ALTER / CREATE)

New PHP files only where necessary

Minimal HTML integrated into existing layouts

Do NOT include explanations unless necessary for clarity